// ── FILE: include/config.h ───────────────────────────────────────────────────
/*
Edit the following placeholders before build.
*/
#pragma once

// ── WiFi ──
#define WIFI_SSID        "MinhThu-Lau"
#define WIFI_PASSWORD    "0907133990"

// ── OpenAI (ChatGPT) ──
// Create an API key at https://platform.openai.com/  (keep it secret!)
#define OPENAI_API_KEY   "${OPENAI_API_KEY}"
// Model for chat: choose a lightweight reasoning-capable model
#define OPENAI_CHAT_MODEL  "gpt-4o-mini"

// ── Speech (optional) ──
// Whisper (speech-to-text). We'll call /v1/audio/transcriptions
#define OPENAI_USE_WHISPER   0
// TTS voice (text-to-speech). We'll call /v1/audio/speech with format=wav
#define OPENAI_TTS_VOICE     "alloy"  // pick a voice available

// ── Weather ──
// Get an API key at https://openweathermap.org/api (Free tier works)
#define OWM_API_KEY "your_openweather_key"
// Either set a city & country code OR fixed lat/lon.
#define OWM_CITY    "Bao Loc"
#define OWM_COUNTRY "VN"
//#define OWM_LAT   "11.547"
//#define OWM_LON   "107.807"

// ── Display (round LCD) pins & type using Arduino_GFX ──
// Common XiaoZhi round panel uses GC9A01 240x240 SPI
#define LCD_DRIVER_GC9A01 1
#define LCD_WIDTH   240
#define LCD_HEIGHT  240
// SPI pins (edit to your board wiring)
#define LCD_SCK   12
#define LCD_MOSI  11
#define LCD_MISO  -1
#define LCD_DC     9
#define LCD_CS    10
#define LCD_RST    8
#define LCD_BL    14   // backlight (PWM capable)

// ── I2S Mic (e.g., INMP441/IIS mic) ──
#define I2S_MIC_WS   2   // LRCLK
#define I2S_MIC_SCK  1   // BCLK
#define I2S_MIC_SD   3   // DOUT from mic

// ── I2S Speaker/AMP ──
#define I2S_SPK_WS   5
#define I2S_SPK_SCK  6
#define I2S_SPK_SD   4   // DIN to amp

// ── Buttons ──
#define BTN_TALK     0   // hold to talk

// ── General ──
#define DEVICE_NAME  "LONG GPT"
